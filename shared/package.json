{
  "name": "shared",
  "private": true,
  "scripts": {
    "dev": "prisma generate",
    "build": "prisma generate",
    "generate": "prisma generate",
    "push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1"
  },
  "devDependencies": {
    "prisma": "^5.7.1"
  }
}

{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:nextjs": "cd nextjs && pnpm run dev",
    "build:nextjs": "cd nextjs && pnpm run build",
    "dev:ws": "cd websocket && pnpm run dev",
    "start:ws": "cd websocket && pnpm run start",
    "deploy:ws": "cd websocket && pnpm run deploy"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.13",
    "postgres": "^3.4.3"
  }
}